import { MasterServiceDesk } from "@/components/master/MasterServiceDesk";

export default function MasterVisasPage() {
  return (
    <MasterServiceDesk
      kind="VISA"
      title="Visas"
      subtitle="Queries, unpaid invoices, paid visa files, and visa catalog — not bus tickets."
    />
  );
}
