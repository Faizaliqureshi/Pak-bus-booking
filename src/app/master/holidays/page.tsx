import { MasterServiceDesk } from "@/components/master/MasterServiceDesk";

export default function MasterHolidaysPage() {
  return (
    <MasterServiceDesk
      kind="HOLIDAY"
      title="Holidays"
      subtitle="Holiday queries, invoices, paid tours, and editable catalog."
    />
  );
}
